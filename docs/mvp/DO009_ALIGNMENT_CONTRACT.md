# DO-009 alignment contract

Candidate expected events must fall within the versioned early/late window. Exact pitch is
preferred, then smallest absolute timing delta, then stable observed ID. Loop identity is
`(expected_event_id, repetition_index)`; canonical IDs are never modified. Outputs are structural
statuses and signed deltas. Correctness, grades, mastery, and coaching are prohibited.
